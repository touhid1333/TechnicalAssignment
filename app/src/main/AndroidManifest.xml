<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.touhid.technicalassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechnicalAssignment">
        <activity
            android:name=".views.AddProductActivity"
            android:label="Add Product" />
        <activity
            android:name=".views.SignUpActivity"
            android:theme="@style/Theme.TechnicalAssignment.NoActionBar" />
        <activity android:name=".views.SignInActivity" android:theme="@style/Theme.TechnicalAssignment.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".views.MainActivity">
        </activity>
    </application>

</manifest>